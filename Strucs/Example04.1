/*Crie uma estrutura representando os alunos de um determinado curso. A estrutura deve
conter a matrıcula do aluno, nome, nota da primeira prova, nota da segunda prova e nota
da terceira prova.
(a) Permita ao usuario entrar com os dados de 5 alunos.
(b) Encontre a maior nota.*/

#include <stdio.h>

struct alunos{
    int matricula, n1[3],n2[3],n3[3],n4[3],n5[3];
    char nome[100];
};

int main (void){
    struct alunos InfoAlunos[5];
    int MaiorNota = 0;
    for (int i = 0; i < 5; ++i){
        
        printf("-Digite o nome do aluno %d: ", i+1);
        fgets(InfoAlunos[i].nome, sizeof(InfoAlunos[i].nome), stdin);
        
        printf("-Digite a matricula do aluno %d: ", i+1);
        scanf("%d", &InfoAlunos[i].matricula);

            for (int j = 0; j < 3; ++j){
            printf("-Digite a nota %d desse aluno: ", j+1);
            scanf("%d", &InfoAlunos[i].n1[j]);
            
                if (MaiorNota < InfoAlunos[i].n1[j]){
                MaiorNota = InfoAlunos[i].n1[j];
                }
                    while(getchar()!= '\n');
        }
        printf("\n");
    }
    printf("%d", MaiorNota);
    return 0;
}
